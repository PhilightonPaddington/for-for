package main

import "fmt"

func main() {
	var a, b, x, y int
	fmt.Scan(&a, &b)
	j := 10000
	for a > 0 {
		x = a / j
		a = a % j
		y = b
		for y > 0 && x > 0 {
			if x == y%10 {
				fmt.Print(x, " ")
			}
			y = y / 10
		}
		j = j / 10
	}
}
